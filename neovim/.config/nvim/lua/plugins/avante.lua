-- Minimal placeholder to satisfy `require('plugins.avante')` when the
-- upstream avante.nvim plugin is intentionally disabled. This prevents
-- Lazy/Vim from erroring with "failed to load plugins.avante".

return {
  -- No-op plugin spec: keep the module present so imports succeed.
}
