version: v1
providers:
  - id: docker
    name: Docker (lokal)
    type: docker
    default: true
    settings:
      socketPath: /var/run/docker.sock

  - id: talos-cluster
    name: Kubernetes Talos Cluster
    type: kubernetes
    settings:
      namespace: devpod
      context: my-dev-cluster
      storageClass: standard

  # - id: ec2-prod
  #   name: AWS EC2 Prod
  #   type: ssh
  #   settings:
  #     host: ec2-1-2-3-4.compute.amazonaws.com
  #     user: ubuntu
  #     port: 22
  #     # Använd antingen path till nyckel eller inline nyckelmaterial
  #     identityFile: ~/.ssh/devpod.pem
  # identity: |
  #   -----BEGIN OPENSSH PRIVATE KEY-----
  #   ...
  #   -----END OPENSSH PRIVATE KEY-----

  # - id: codespaces
  #   name: GitHub Codespaces
  #   type: codespaces
  #   settings:
  #     machine: basicLinux32gb
  #     region: westeurope
